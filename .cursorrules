# General Rules

- **OS/Terminal**: always use windows (and not unix) terminal commands.
- **Command Chaining**: especially avoid using "&&". Use separate commands or standard PowerShell syntax instead.
